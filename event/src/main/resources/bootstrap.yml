spring:
  config:
    import: "configserver:"
  application:
    name: event
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: event
      profile: dev

springdoc:
  swagger-ui:
    path: /auth/swagger-ui.html

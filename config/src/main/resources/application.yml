spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/glaptev8/letsToGether
          searchPaths: config
          clone-on-start: true

server:
  port: 8080
spring:
  config:
    import: "configserver:"
  application:
    name: gateway
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: gateway
      profile: dev
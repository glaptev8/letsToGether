spring:
  config:
    import: "configserver:"
  application:
    name: eureka
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: eureka
      profile: dev
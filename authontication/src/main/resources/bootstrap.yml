spring:
  config:
    import: "configserver:"
  application:
    name: authentication
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: authentication
      profile: dev
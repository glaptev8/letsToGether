spring:
  config:
    import: "configserver:"
  application:
    name: authentication
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080
      name: authentication
      profile: dev
  profiles:
    active: dev



spring:
  config:
    import: "configserver:"
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: authentication
      profile: dev

springdoc:
  swagger-ui:
    path: /auth/swagger-ui.html

upload:
  path: /Users/glelap/projects/letsToGether/authentication/avatar/